# Local TTS Server — Chatterbox Turbo backend
# Install: pip install -r requirements-chatterbox.txt
#
# For GPU (CUDA 12.x):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-chatterbox.txt
#
# For CPU-only (slower, for prototyping):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-chatterbox.txt

# --- Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- TTS Model ---
# Chatterbox Turbo (Resemble AI) — high-quality zero-shot voice cloning
chatterbox-tts

# --- Utilities ---
soundfile>=0.12.0
numpy>=1.24.0
pydantic>=2.0
httpx>=0.27.0
